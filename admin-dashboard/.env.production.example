# Connection string for Axios requests targeting Backend
# Point this to the live Heroku/Railway/Render deployment URL explicitly mapping to HTTPS
NEXT_PUBLIC_API_URL=https://api.repargo.com

# Used for triggering native WebSockets natively connecting to `io()`
NEXT_PUBLIC_SOCKET_URL=https://api.repargo.com
